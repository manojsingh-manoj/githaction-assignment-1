# Environment configuration
environment = "dev"

# VM size (can be overridden based on environment)
vm_size = "Standard_D2s_v3"

# Ports to open in the network security group
open_ports = [22, 80, 8080]